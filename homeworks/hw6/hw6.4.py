import random
# **Задача 4: Проверка доступа к контенту с учётом блокировки**
# Условие:
# Пользователь может получить доступ к контенту, если:
# Он подписан на платную подписку И его возраст больше 18 лет
# ИЛИ Он является администратором.
# А так же ко всем условиям добавить проверку на: Заблокирован ли его аккаунт (он не должен быть заблокированным чтобы получить доступ + заблокированным может быть даже админ)
#
# Что должна сделать программа:
# - Запрашивает у пользователя 4 параметра – определить их и их тип нужно самостоятельно
# - Вывести, есть ли у пользователя доступ по введенным значениям

# Запрос значений для каждого флага
print(f"Введение значений:")
# is_Subscribed = bool(int(input("Является ли пользователь подписчиком? (1 - True, 0 - False): ")))
# is_Adult = bool(int(input("Является ли пользователь совершеннолетним? (1 - True, 0 - False): ")))
# is_Admin = bool(int(input("Является ли пользователь администратором? (1 - True, 0 - False): ")))
# is_Blocked = bool(int(input("Заблокирован ли пользователь? (1 - True, 0 - False): ")))
#
# # Вывод введенных значений для проверки
# print(f"Введенные значения: Подписка: {is_Subscribed}, Совершеннолетие: {is_Adult}, Админ: {is_Admin}, Блокировка: {is_Blocked}")

# #Рандомная генерация чисел для задачи
is_Subscribed, is_Adult, is_Admin, is_Blocked = random.choice([True, False]), random.choice([True, False]), random.choice([True, False]), random.choice([True, False])
print(f"Введенные значения: Подписка: {is_Subscribed}, Совершеннолетие: {is_Adult}, Админ: {is_Admin}, Блокировка: {is_Blocked}")

if is_Blocked:
    # Если пользователь заблокирован, доступ запрещен
    print(f"Доступ запрещен: Пользователь заблокирован (is_Blocked = {is_Blocked}).")
else:
    if is_Admin:
        # Если пользователь является администратором, доступ разрешен
        print(f"Доступ разрешен: Пользователь является администратором (is_Admin = {is_Admin}).")
    elif is_Subscribed and is_Adult:
        # Если пользователь подписан и совершеннолетний, доступ разрешен
        print(f"Доступ разрешен: Пользователь подписан и совершеннолетний "
              f"(is_Subscribed = {is_Subscribed}, is_Adult = {is_Adult}).")
    else:
        # Во всех остальных случаях доступ запрещен
        print(f"Доступ запрещен: Пользователь не удовлетворяет условиям доступа "
              f"(is_Subscribed = {is_Subscribed}, is_Adult = {is_Adult}, is_Admin = {is_Admin}).")